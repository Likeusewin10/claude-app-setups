# 用 Claude Code 一键装好 <应用名>

> 这是给 **Claude Code** 读并逐步执行的指令文档（playbook）。
> 面向**完全新手**：用户只装了 Claude Code，其余全部由你（Claude Code）代劳。
> **本文件内不含任何密钥**，全程读环境变量；可安全公开托管 / 被 curl 抓取。

<!--
作者填写指南（写完删掉本段注释）：
- 保持下面的章节顺序与标题风格，Claude Code 才能稳定执行。
- 凭证一律走 env 变量（如 $XXX_API_KEY），绝不把 sk-... 写进本文件。
- 每个「第 N 步」末尾必须有“校验门”，明确什么算通过、不过怎么排查。
- 只有真正需要人类操作（建应用/授权/扫码/粘贴凭证）才停下来引导用户。
- 在仓库 catalog.json 里登记 requiredEnv，与本文件用到的 env 变量一致。
-->

## 给 Claude Code：执行总则

- 你的目标：把 <应用> 装好、配好（模型/凭证）、接好（渠道）、最后**自我验证打通**。
- 平台假设 macOS / Linux；Windows 见末尾「Windows 备注」。
- **逐步执行，每步带校验门**：不过不要进入下一步，先看日志排查并把结论告诉用户。
- 先确认所需 env 已设置（按本应用需要替换）：`printf '%s' "${XXX_API_KEY:+set}"`。为空则引导用户回落地页 `export XXX_API_KEY=...` 后重来。

## 第 0 步 · 环境预检

```bash
# 检查运行时/依赖版本
```

校验门：<写明什么算通过>。

## 第 1 步 · 安装

```bash
# 官方安装命令（优先非交互）
```

校验门：<可执行文件能打印版本号 等>。

## 第 2 步 · 配置（模型 / 凭证）

```bash
# 非交互配置；凭证读 env 变量
```

校验门：<一次最小探活成功>。

## 第 3 步 · 接入渠道（如需）

需要用户操作的部分（建应用、授权、扫码）在这里清楚引导，并给官方链接。
能自动的（写配置、重启服务）你自己跑。

校验门：<渠道连接就绪>。

## 第 4 步 · 端到端自验证（成功标准）

```bash
# 观察日志 / 发一条测试消息 / 确认回复
```

满足即向用户报告成功，并给出常用指令。

## 排障速查

- <常见问题 1 → 处理>
- <常见问题 2 → 处理>

## Windows 备注

<PowerShell 安装命令与差异>

## 参考（官方文档）

- <官方安装/配置/渠道文档链接>
